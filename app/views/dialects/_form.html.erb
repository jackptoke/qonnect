<%= form_with(model: dialect, local: true) do |form| %>
  <% if dialect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dialect.errors.count, "error") %> prohibited this dialect from being saved:</h2>

      <ul>
        <% dialect.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dialect_name %>
    <%= form.text_field :dialect_name %>
  </div>

  <div class="field">
    <%= form.label :language_id %>
    <%= form.text_field :language_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
